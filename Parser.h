#ifndef PARSER_H_INCLUDED
#define PARSER_H_INCLUDED

/*class Parser
{
public:
    void parseForStyle(string word)
    {
        string temp
        if(line.find("**") != std::npos)
        {
            word.replace(line.find("**"), line.find("**") + 2, "<b>");
            word.replace(line.rfind("**"), line.rfind("**") + 2, "</b>");
        }
    }
};*/
#endif // PARSER_H_INCLUDED
